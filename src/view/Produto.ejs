<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Produtos</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
        }
        .menu {
            font-size: 16px;
        }
        .produto {
            border: 1px solid #ccc;
            padding: 10px;
            margin: 10px 0;
            display: flex;
            align-items: center;
        }
        .produto img {
            max-width: 50px;
            margin-right: 10px;
        }
        .produto h2 {
            font-size: 18px;
            margin: 0;
        }
        .produto p {
            margin: 5px 0;
        }
        .produto input[type="text"] {
            display: none; /* Esconder input de descrição */
        }
    </style>
</head>
<body>
    <div class="menu">
        <h2>Lista de Produtos</h2>
        <form action="/add" method="post">
            <% for (let i = 0; i < tabela.rowCount; i++) { %>   
                <div class="produto">
                    <img src="../imagem/<%= tabela.rows[i].imagem %>" alt="<%= tabela.rows[i].descricao %>">
                    <div>
                        <a href="/ListarProdutos/<%= tabela.rows[i].codigo %>" target="principal">
                            <h2><%= tabela.rows[i].descricao %></h2>
                        </a>
                        <p id="AddPreco" name="AddPreco">Preço: R$ <%= parseFloat(tabela.rows[i].preco).toFixed(2) %></p>
                        <p>Quantidade: <%= tabela.rows[i].qtde %></p>
                        <input type="text" value="<%= tabela.rows[i].codigo %>" id="ADDcodigo" name="ADDcodigo">
                        <input type="number" min="1" id="ADDqtde" value="1" name="ADDqtde">
                        <input type="submit" value="ADD CARRINHO">
                    </div>
                </div>
            <% } %>
        </form>
    </div>
</body>
</html>
